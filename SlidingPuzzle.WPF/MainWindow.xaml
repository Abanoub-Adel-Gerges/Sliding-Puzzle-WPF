<Window x:Class="SlidingPuzzle.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sliding Puzzle Master" Height="650" Width="950" 
        Background="#F5F7FA" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center">
            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.1"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Puzzle Board" FontSize="18" FontWeight="Bold" Foreground="#2D3748" Margin="0,0,0,15" HorizontalAlignment="Center"/>
                    <Border BorderBrush="#E2E8F0" BorderThickness="2" CornerRadius="10" 
                            HorizontalAlignment="Center" VerticalAlignment="Center" Background="#EDF2F7" Padding="5">
                        <UniformGrid x:Name="PuzzleGrid" />
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>

        <Grid Grid.Column="1" Margin="25,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Height" VerticalAlignment="Center" Foreground="#718096"/>
                        <TextBox Grid.Column="1" x:Name="TxtH" Text="3" Width="45" Padding="5" Margin="5,0"/>
                        <TextBlock Grid.Column="2" Text="Width" VerticalAlignment="Center" Foreground="#718096" Margin="10,0,0,0"/>
                        <TextBox Grid.Column="3" x:Name="TxtW" Text="3" Width="45" Padding="5" Margin="5,0"/>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Timeout (sec) [1-60]" VerticalAlignment="Center" Foreground="#718096"/>
                        <TextBox Grid.Column="1" x:Name="TxtTimeout" Text="5" Width="45" Padding="5" Margin="5,0"/>
                    </Grid>
                    <Button Content="Shuffle &amp; Play" Click="NewGame_Click" Style="{StaticResource ModernButtonStyle}" Background="#48BB78" Margin="0,10,0,5"/>
                    <TextBlock Text="Algorithm" VerticalAlignment="Center" Foreground="#718096" Margin="0,10,0,5"/>
                    <ComboBox x:Name="CboAlgorithm" Margin="0,0,0,15" Padding="5" SelectedIndex="0">
                        <ComboBoxItem Content="A* Search" Tag="AStar"/>
                        <ComboBoxItem Content="Greedy Search" Tag="Greedy"/>
                        <ComboBoxItem Content="Uniform Cost (UCS)" Tag="UCS"/>
                    </ComboBox>
                    <Button Content="Solve Auto" Click="Solve_Click" Style="{StaticResource ModernButtonStyle}" Background="#4299E1"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Solution Path" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ListBox x:Name="ListPath" BorderThickness="1" BorderBrush="#E2E8F0" Background="#F8FAFC" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#EBF8FF" CornerRadius="4" Margin="2" Padding="6,3">
                                    <TextBlock Text="{Binding}" Foreground="#2B6CB0" FontWeight="Bold" FontSize="12"/>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Row="2" Background="#2D3748" CornerRadius="12" Padding="15" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="Status" Foreground="#A0AEC0" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TxtStats" Text="Ready! Click tiles to move." Foreground="White" FontSize="13" TextWrapping="Wrap" LineHeight="18"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>